name: cap-table-fast-assess
session: ../sessions/default.yaml
fidelity: cowork
on_unanswered: fail
skills: [cap-table]                # 0.5.0: scope the staleness hash to this skill + shared roots (F-6)

# P2-c — fast-assess routing (Phase O). SHORT directional question, no document, and NONE of the
# full-pipeline trigger words (full review / report / explorer / counsel / deep dive) → must route to
# fast-assess (<60s), write the fast_assess_only.json sentinel, and SKIP the full pipeline.
prompt: |
  Run a quick cap-table assessment for Cadence (US Delaware seed-stage B2B SaaS). I have one $500,000
  post-money SAFE at a $10,000,000 cap and I'm about to raise a $4,000,000 round at a $16,000,000
  pre-money. Use the cap-table skill to give me a directional read on my founder dilution — just the
  fast assessment, I don't need the full review, report, or explorer yet.

answers:
  # Fast-assess sometimes gathers cap structure via a (multi-question) gate before the directional
  # read; answer any such gate with option 1 so the run is deterministic.
  - when_question: ".*"
    choose: first

assert:
  - result: success
  # Fast-assess sentinel proves Phase O ran (path confirmed at discovery; -fastassess slug dir).
  - file_exists: outputs/artifacts/cap-table-cadence-fastassess/fast_assess_only.json
  # Full pipeline skipped: no analytical sub-agent dispatch in fast-assess.
  - dispatch_count_max: 0
  - transcript_no_host_path: true
